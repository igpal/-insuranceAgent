import Vue from 'vue';
import Vuex from 'vuex';
import interfaceStore from './interfaceStore';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {},
  mutations: {},
  actions: {},
  modules: { interfaceStore }
});
